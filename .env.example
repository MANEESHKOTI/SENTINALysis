# Port for the API service
API_PORT=8000

# Path to the model artifacts inside the container
MODEL_PATH=/app/model_output

# Port for the UI service
UI_PORT=8501

# API URL for the UI to talk to
API_URL=http://api:8000
